<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>
            {% block title %}
                    Garage V-Perrot
            {% endblock %}
        </title>
    </head>
    <body>
  <h1>Demande de 
  {{ contact.nom }} {{ contact.prenom }}</h1>

  <h2> {{ contact.voiture }}</h2>
<p> Email : {{ contact.email }}</p>
<div> {{ contact.sujet }}</div>
<div> {{ contact.message }}</div>

    </body>
</html>
